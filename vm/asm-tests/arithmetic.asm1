byte 1;
byte 2;
byte 3;

// Program starts here
add(*0, *1);
mul(*0, *2); // and this
add(*0, 111); // should result in 120
div(*0, 10); // should result in 12
exit(*0);

// Syntactically, these should work as well:
//_123(*123); // and this
//_(*123,*123); // and this
//ancd123BDAF(*123,      1000); // and this
